# 更新记录

按日期记录每次用户能感觉到的改动（新功能、修复、界面调整），不记录纯内部代码整理。最新的记录放在最上面。

## 2026-08-23（修复：网络不好时会莫名其妙冒出一堆"待同步"）

- 修复：什么都没操作，顶部却突然显示几条、甚至几千条"待同步"——根源是网络不稳定时，两轮后台自动同步偶尔会前后重叠着跑，一轮刚结束时会不小心把还在进行中的另一轮也一起标记成"结束"，导致那一轮从云端拉回来的数据被误当成本地新改动，又重新排队等着推送上去（这些误判的记录大多数会在下一轮自己同步成功、消失，但网络越差、重叠越频繁，堆积就越明显）。现在同一时间只允许一轮同步真正在跑，重叠的那一轮会直接跳过，不会再互相干扰。
- 新增：「···更多」里加了「同步详情」，能看到哪些记录还没同步成功、重试了多少次；重试次数明显偏多的会标红并且显示具体的失败原因，方便下次再遇到同步卡住时自己先看一眼是什么问题。
- 新增：「同步详情」里每条记录可以手动"放弃同步"——适合确定同步不了的卡住记录，本地数据不会被删，只是这次改动以后不会再同步给其他设备/家人。
- 改进：「同步详情」每条记录现在会显示具体是哪一条（比如行程项的名字、账目的备注），不再只显示"账目·新增/修改"这种看不出是哪一笔的描述。

## 2026-08-23（改进：一天行程很长时，往下滑不会再看不见自己在看哪一天）

- 改进：时间线视图往下滑动时，顶部那排日期条以前会跟着内容一起滚走，现在会整条固定在顶部（缩成只有日期数字、不带"周几"的小方块），还能直接点其他日期切换，不用先滑回顶部。
- 这是对8月22日同一功能第一版的重新设计——第一版做成"缩成一个小胶囊"的效果，实际使用时会跟下面的行程卡片叠在一起，点击返回顶部还有画面跳动的问题，这次改成"整条日期条原样吸顶"，不做变形动画，更稳定。
- 修复：吸顶栏刚出现时顶部会露出一条空白缝隙，划过去的行程卡片会从缝里透出来（点上去还真的能打开那张卡片）——根源是滚动区域自己带了顶部内边距，吸顶栏只会贴到内边距内侧、贴不到最顶上，现在把这段内边距挪到日期条自己身上，缝隙就没有了。
- 修复：吸顶显示精简日期条时，点别的日期能正确跳转行程内容，但如果那天在完整日期条里本来就不在视野内，划回顶部后完整日期条还停在原来的位置，选中的那天要自己划过去找——现在选日期时会自动把两条日期条都滚到能看到当前选中日期的位置。

## 2026-08-22（新增：行程项可以换到别的日期，不用删了重打）

- 新增：点开一个行程项卡片编辑时，多了一个"日期"选项，选别的日期保存后，这一项就会挪到新的那天，不用删掉重新输入一遍。
- 如果这个行程项关联了一笔账目：普通单日账目会跟着一起换到新日期，避免行程项和账目对不上号；如果关联的账目做了"跨天分摊"（比如横跨好几天的住宿），换日期时会先提醒你——那笔账目分摊到每一天的日期不会跟着自动调整，需要自己去记账页确认。

## 2026-08-21（新增：三个容易被忽略的入口，第一次看到时加了发现提示小红点）

- 新增：汇率簿（记账页右上角"簿"字按钮）、想去的地点（行程页书签图标、我的行程列表页胶囊按钮）、导出与分享（"···"按钮）这三个入口，第一次打开一趟行程时会带一个小红点提醒"这里有功能"。点一下对应按钮，那个人就再也不会看到这个提醒——按人记录，不是按设备，换手机登录同一个账号也不会重复提醒（除非真的没点过）。

## 2026-08-21（新增："···更多"面板里加了"使用指南"入口）

- 新增：行程详情页"···更多"面板里加了一条"使用指南"，点开是一份图文并茂的说明——照真实界面截图讲了行程/记账/分账/预算/想去的地点/汇率簿/导出分享这7个功能怎么用。

## 2026-08-21（修复：部分iPhone分享出来的Google Maps链接解析不出坐标）

- 修复：某些Google Maps分享链接（这次是iPhone上分享出来的一个例子）贴进"想去的地点"/行程地点框会提示解析失败。原因是这类链接既没有地图标记的坐标、也没有常见的"/place/地址"格式，地址信息藏在链接一个不常见的参数里，之前的解析逻辑认不出这种款式，现在补上了。

## 2026-08-21（修复：金额有小数时，记账列表会四舍五入成整数显示）

- 修复：记一笔时输入了带小数的金额（比如509.6），之前记账列表、团队总花费这些地方都会四舍五入显示成整数（RM510），看起来像是输错了或者存错了——实际存的数字是对的，只是显示的时候被简化了。现在有小数的金额会照实显示两位小数（RM509.60），整数金额还是照旧不显示小数，保持干净。
- 顺手修复：用外币记账、按汇率换算成本地货币时，换算结果偶尔会带上浮点数计算带来的极小误差（比如509.60000000000002），之前被"四舍五入成整数"顺带盖住了，现在既然要照实显示小数就先把这类误差清理掉，不会让用户看到奇怪的长串小数。

## 2026-08-21（修复：分账页"去结算"里的日期，窄屏下文字会换行压到日历图标上）

- 修复："费用分摊·按人结算"里点"去结算"展开的表单，金额和日期两个输入框并排各占一半宽度，日期"2026年8月21日"这串字太长，之前会换行，"日"字换到第二行、跟旁边的日历图标叠在一起。现在这段文字固定不换行，太窄的时候优先保证不叠字。

## 2026-08-21（修复：日期选择器会跑出屏幕，右边一整列日期看不见）

- 修复：点"返程日期"这类靠右的日期栏，弹出来的日历会冲出屏幕右边，最右边一整列日期（周六周日）根本看不到，页面还会变得能左右拖动。原因是日历宽度写死 280px，却从输入框的左边开始展开——而"出发日期/返程日期"是两列并排的，右边那列只有一半宽度，放不下。靠近屏幕底部时同理，它以前只会往下弹，不管下面还剩多少空间。
- 现在日历打开时会先算好位置：右边放不下就整体往左挪（两边各留 8px），下面放不下就改成往上弹。空间充足时的样子跟以前完全一样。
- 影响范围：新建/编辑行程、记一笔、结算这三处的日期选择都受益，不只是行程表单。

## 2026-08-21（修复：切到另一个团队后头像变"?"、而且没问"你是谁"）

- 修复（真机测到的）：切换到另一个团队之后，APP 没有问"你是谁？"就直接进去了，左上角头像显示成一个"?"。原因是"记住上次用的身份"这个值在换成按团队分开存时，留了一个读旧格式的兜底——本意是让老用户升级后不用重选一次，但那个兜底对**每个**团队都会生效，于是切过去的时候它把上一个团队的身份直接拿来用了，而那个人在新团队里并不存在。
- 加固：现在 APP 每次都会确认"记住的这个身份在当前团队里真的存在"，不存在就正常回到"你是谁？"让你选。这也让已经出问题的手机自己恢复，不需要清数据或重装。同一个加固对"记住的行程"早先已经做过，这次补上了身份这一半。

## 2026-08-21（修复：切换团队后要退出APP重进才生效）

- 修复：点了切换团队之后界面没反应，那一行还显示原来的团队名，必须完全退出APP重新打开才生效。原因是换团队相当于把APP的地基换掉了（本地数据刚被清空重建、当前身份和当前行程都属于另一个团队），第一版试图逐个更新这些状态，漏掉了好几处。现在切换成功后APP会自己重载一次——你原来手动"退出重进"的动作，现在它自动做了。
- 修复（更严重，但还没来得及暴露）：服务端已经按"你选的那个团队"判断权限了，客户端却还在自己算"最早加入的团队"。两边不一致的话，切到另一个团队之后新记的账会被打上错误的团队标记、然后被服务器拒绝，表现成"这笔一直同步不上去"。现在客户端直接问服务端要答案，不再自己算一套。

## 2026-08-21（新增：一个人可以同时属于多个团队，并在它们之间切换）

- 新增：如果你同时属于两个以上团队，「我的行程」页面标题下面会多出一行显示当前团队，点它可以切换。**只属于一个团队的话什么都不会多出来**——目前所有人都是这种情况，界面完全不变。
- 说明：每个团队的行程和账目完全独立，互相看不到。切过去会重新下载那个团队的数据（中间会显示"正在切换到 XX"，网络慢时要等几秒），切回来同理，什么都不会丢。
- 说明：**各团队分别记住你上次看的行程和身份**——切回来还停在原处，不用每次重选。
- 说明：如果还有记录没同步上去，切换会被暂时拦住并说明原因。这是必须的：没推上去的记录带着旧团队的标记，切过去会被服务器永久拒绝、那几条就丢了。等顶部显示"已同步"再切即可。
- 说明：新建团队暂时还是人工流程（找我开），这次只做"在已有的几个团队之间切换"。同一个账号在多台设备上登录时，切换是跟着账号走的——在手机上切了，电脑上下次同步也会跟着切过去。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0018_active_household_pointer.sql`）才能生效。

## 2026-08-21（修复：一个邮箱同时属于多个团队时，会看到哪个团队是不确定的）

- 修复（潜伏的数据安全隐患）：判断"你属于哪个团队"的那段逻辑，在一个邮箱同时属于多个团队的情况下，返回哪个团队是**不确定的**——数据库和APP两边各自独立地"随便挑一个"，可能挑到不同的团队，结果就是新记的数据会被数据库静默拒绝写入、表现成"这条同步不上去"。现在两边都按"最早加入的团队"这个确定的规则来挑。
- 说明：**今天不会有任何可见变化**。目前每个邮箱都只属于一个团队，加了排序规则之后挑出来的还是同一个，完全等价——这次修的是一个一直潜伏、等到真有人同时属于两个团队才会爆的问题。
- 说明：这次刻意**没有**做"同时属于多个团队并能来回切换"这个能力（那需要新增当前团队指针、切换界面，并动到所有表的权限策略，属于影响面大得多的改动，留待以后单独讨论）。这次只是先把"挑哪一个"变成确定的。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0017_deterministic_current_household.sql`）才能生效。

## 2026-08-21（修复：行程项卡片点开编辑后，按手机返回键会退出APP）

- 修复：点开一个行程项卡片进入编辑状态后，按手机的返回键会直接退出整个APP。现在返回键会正确收起编辑框，行为跟点击外面（有内容自动保存，没内容直接放弃）一致。

## 2026-08-21（改进：行程项卡片点开编辑后，点外面就能收起，不用非得点"✕"）

- 改进：点开一个行程项卡片进入编辑状态后，以前只能靠底部的"✕"按钮退出。现在点卡片外面任何地方也能收起——如果标题栏已经填了内容，会跟点"✓"一样自动保存；标题栏还是空的（比如刚点开"添加行程项"又反悔），点外面就直接放弃，不会留下一条空的行程项。

## 2026-08-21（新增：行程详情页里也能直接打开"想去的地点"）

- 新增："想去的地点"以前只能从"我的行程"列表页进，一旦点进某一趟具体行程就再也找不到。现在"行程"tab的头部（时间线/日历/地图切换栏旁边）常驻加了一个紫色书签图标，不管切到哪个视图都能点，不用退出这趟行程就能看/加想去的地点。只在"行程"tab出现，"记账/预算/分账"不受影响。

## 2026-08-21（修复：在"想去的地点"页面按手机返回键，会直接退出APP）

- 修复：从"我的行程"列表点"想去的地点"进去之后，按手机的返回键会直接退出整个APP，而不是像别的弹出页面（比如"汇率簿"）那样先关掉当前页面。原因是这个新页面漏了一行"告诉返回键该怎么处理"的代码。

## 2026-08-21（修复：附近想去的地点，提示逻辑跨天串了，备注也没带过来）

- 修复：跨城市的行程（比如"东京+北海道"），之前不管切到哪一天，提示卡都会把整趟行程里离任何一天足够近的地点都列出来——结果看北海道那几天时，东京附近的地点也会被推荐，隔着几百公里毫无意义。现在只看当前选中这一天已经排上时间线的点算距离，别的天不会串过来。
- 修复：提示卡上一直没显示这条地点自己的备注；点"+"直接加进当前这天，或者从"从想去的地点里选一个"弹层里选，备注也都没带过去，加进去的行程项是空的。现在提示卡上会显示备注（如果有），两种加入方式也都会把备注带过来。

## 2026-08-21（修复：行程时间线"附近想去的地点"提示卡，长地址会把"+"按钮挤到看不见）

- 修复：提示卡里的chip，地点名字/地址比较长时（比如完整的门牌地址），"+"按钮会被挤到chip最右边，得在chip内部横向拖动才能碰到，很不顺手。现在chip限制了最大宽度，名字过长会截断显示省略号，"+"按钮永远紧跟在旁边、不用拖动就能点到。

## 2026-08-21（修复：贴某些Google Maps分享链接会"解析失败"）

- 修复：贴地点/新增"想去的地点"时，某些Google Maps分享链接会提示"解析失败"——这类链接是Google用内部编号指代地点，而不是直接把经纬度写进网址里的款式，之前的解析逻辑认不出这种款式。
- 新增：这种链接解析不出坐标时，现在会拿链接里认得出来的地址再查一次（用Google自己的Geocoding服务，比免费地图数据库覆盖更全），大部分之前失败的链接现在应该能成功了。
- 部署要求：需要在 Vercel 后台配置一个新的环境变量 `GOOGLE_GEOCODING_API_KEY`（用量很小，正常情况下在Google每月的免费额度内，不产生费用）。

## 2026-08-20（改进：复制邀请码现在会带上说明文案 + 图文教程链接）

- 改进：点"复制邀请码"，以前只会复制一串裸码（比如`X7K9M2QPRT`），朋友收到不知道该干什么。现在复制的是一整段能直接发给对方的话——包含"旅记是什么"的一句介绍、去哪里打开、点哪个按钮、填邮箱+邀请码，邀请码本身也在里面。
- 新增：复制的文案末尾还带了一步步图文教程的链接（`/join-guide.html`）——照着截图对着点，卡在哪一步都能自己查。这个教程原来是内部文档，这次把它挪到能被浏览器直接打开的位置，才能真正发给别人用。
- 说明：按钮本身还是"复制邀请码"，弹层里给邀请人自己看的那句说明文字也没变，只是点了之后复制到剪贴板的内容变丰富了。

## 2026-08-20（新增："想去的地点"——跨行程的收藏清单）

- 新增："我的行程"列表页右上角加了个"想去的地点"入口——平时刷到想去的餐厅/景点可以先记下来（名字+地点+备注），不属于任何一趟具体行程，长期攒着，规划任何一趟新行程都能用。
- 新增：记行程项时，地点输入框上方多了"从想去的地点里选一个"，点开是带筛选框的弹层，选一条会自动把名字和坐标带进来，不用重新搜索。
- 新增：安排行程时，如果这份清单里有地点离这趟行程已经排上时间线的某个点比较近（约50公里内），时间线顶部会自动出现一张提示卡"你标记过 N 个附近想去的地点"，点chip上的"+"能直接加进当前选中的这一天；关掉提示卡只是这次不看，下次重新进这趟行程还会再出现。
- 新增：清单里每条地点可以标"还没去/已去过"，跟"有没有排入过行程"是两件独立的事——有的地方可能没通过行程安排就直接去吃了。已经排入过行程的地点会显示"已排入行程 · xxx行程"。
- 说明：这份清单里的地点被删除，不影响已经排进行程里的那些行程项——它们各自保留了自己的名字和坐标，删除清单条目是硬删除+二次确认，不做归档。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0016_wishlist_places.sql`）才能生效。

## 2026-08-20（汇率簿新增：一笔账可以拆成来自不止一批换汇的钱）

- 新增：记账时选汇率，点"这笔钱来自不止一笔汇率？"可以把单选变成多选——比如300块饭钱，一部分来自机场换的那批、一部分来自银行换的那批，各自填一个外币金额就行，不用只能整笔归到一个汇率标签下。
- 新增：每个选中的条目下面会出现一个外币金额输入框，实时提示"刚好分完这笔钱 / 还剩X没分完 / 超出了X"；分完之后会自动算出这笔账的加权平均汇率（按每一批的金额和各自汇率折算，不是简单平均），不用自己拿计算器算。
- 说明：每一批具体用了多少只能自己手动填，不做"按剩余额度自动分"——试算过几种自动分法，算出来的实际花费不一样，没有唯一正确答案，交给自己决定更靠谱。
- 说明：汇率簿里"已用/已换"的进度条、"用过N次"，现在也会把拆分出去的这部分算进去，不会漏算。
- 说明：这是"提前换汇"功能计划中的第二部分（第一部分是记录换汇金额+进度条，已经上线）。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0015_expense_rate_allocation.sql`）才能生效。

## 2026-08-20（修复：安卓上汇率簿页面按返回键会直接退出APP）

- 修复：打开汇率簿之后按安卓的系统返回键，之前会直接退出整个APP，而不是回到记账页——因为这个APP是单页应用，打开汇率簿这个入口一直没有像其他弹层/全屏页那样往浏览器历史里占一条记录，返回键就没东西可"退"，直接冲出了APP。现在跟编辑账目那个全屏页一样接上了返回键，按返回会先关掉汇率簿、回到记账页。
- 说明：如果是在汇率簿里又开了"新增"这类小弹层时按返回键，会直接关掉整个汇率簿页面而不是先关那个小弹层——这种情况比"直接退出APP"好很多，但还不是最精细的处理；真要做到逐层退，需要动到"新增"这类弹层用的公共组件，影响面更大，这次先不做，之后有需要再单独讨论。

## 2026-08-20（修复：明明没用过的汇率，还是显示"用过N次"）

- 修复：汇率簿里两条压根没被任何账目引用过的汇率，却显示"用过1次""用过3次"。根源是"用过N次"以前存的是一个只会往上涨的计数器——记账时用了某个汇率就+1，但这笔账后来被删掉、或者改用了别的汇率，这个计数器不会跟着往下修正，时间久了就跟真实情况完全对不上。现在改成跟"已用/已换金额"那行一样，每次都直接现查真实账目算出来，不再依赖那个会失真的计数器，删账目/改汇率之后"用过几次"会立刻跟着变对。
- 说明：纯内部数据算法调整，界面上"用过N次"这行文字本身没有变化，Supabase 里原来那个字段先留着不动，没有新的数据库迁移。

- 新增：汇率簿页面（记账tab里的"簿"按钮）现在有"+新增"入口，可以直接新建一条汇率，不用非得等到记账时才顺手加。记账时点"新汇率"那个内联表单也是同一套。
- 新增：新增/新建汇率时，可以展开"记录实际换汇金额（可选）"，填"给出多少本位币/换到多少外币"，汇率会自动按这两个数算好（仍可手动改）——不再只是凭感觉估一个汇率数字。
- 新增：记录过换汇金额的条目，汇率簿里会显示"已用 X / 已换 Y（还剩 Z）"外加一条进度条，一眼看出这批换来的钱花得怎么样了；花超了会变红显示"超出多少"。
- 新增：记账时选汇率，如果选中的这批钱已经用完了，chip 会变成醒目的橙色、写"已用完"，选中后还会提示"这批钱已经用完了，仍会按这个汇率记账"——但这只是提醒，不会拦着不让你继续用这个汇率记账，毕竟换汇金额本来很多时候是估算，卡太死反而添麻烦。
- 说明：这是"提前换汇"功能的第一部分。后续还会做"一笔开销可以拆分成来自不止一批换汇的钱"，这次先不涉及。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0014_rate_book_exchange_amount.sql`）才能生效。
- 修复：填"给出/换到"金额自动算出来的汇率之前只保留4位小数，跟汇率簿里其他条目（比如API算出来的）能有7、8位小数比，精度差了一截——日元这类面额大的币种，汇率差一点点摊到大金额上就是实打实的钱。现在改成保留7位小数。
- 修复：编辑一笔已经记过的账、汇率没换，点保存的时候，汇率簿里那条"用过N次"会跟着重复加一次——反复编辑同一笔账，次数会越滚越大，跟真实用过几笔账对不上。现在只有这笔账"新用上"某个汇率（新记的账、或者编辑时换了别的汇率）才会计一次。
- 修复：汇率簿页面"+新增"、编辑时"另存为新标签"这两处新建的汇率，之前会显示"用过1次"，但其实一笔账都还没拿它记过——这两处本来就是纯记录/管理动作，跟记账时顺手新建（那种创建的同一刻就真的用它记了一笔账）不是一回事。现在这两处新建的条目从"用过0次"开始，只有记账时顺手新建的那种才算1次。
- 新增：点"编辑"改一条已有汇率时，现在标签名称和"记录实际换汇金额"也能一起改了——之前编辑只能改汇率数字本身，想改名字得绕道"另存为新标签"（会另外多出一条），换汇金额则完全没地方改。

## 2026-08-20（修复：添加/编辑行程项时，手机上键盘和缩放很别扭）

- 修复：iPhone 上打开行程项的编辑表单，点输入框会自动放大整个页面——Safari 有个规则，输入框字体小于16px时聚焦会自动缩放。第一版改法是把标题、地点、备注这三个框都调到16px，但Android根本没有这个限制，调大之后在Android上反而显得字特别大、跟其他文字比例不搭。现在改成只在iOS Safari上生效的写法，Android/桌面上完全看不出变化，字号跟以前一样。
- 修复：打开编辑表单时，标题输入框原来带了"自动聚焦"，表单一出现键盘就跟着弹出来，跟表单本身的展开动作抢时间，感觉很突兀；关闭表单时同理。现在去掉自动聚焦，键盘只在用户主动点某个输入框时才弹出来。
- 改进：点"添加行程项"或点开某一条编辑时，表单展开后如果比屏幕剩余空间高，保存按钮可能落在屏幕外面，需要手动往下滑才找得到。现在表单一展开就自动滚动到能看见的位置。
- 修复：表单展开、自动滚动到可见位置之后，右下角"记一笔"的悬浮按钮固定在屏幕上，正好糊在表单上面——现在行程表单开着的时候，这个悬浮按钮会先让开。
- 修复：Android 上点标题/地点/备注这几个输入框，键盘上方会跳出一排"密码/银行卡/地址"的自动填充图标，跟这几个字段完全没关系，纯属浏览器认错了——加了 `autocomplete="off"` 之后不会再跳出来。
- 说明：iPhone 的自动缩放行为没法在这边的开发环境里直接重现（那是Safari专属的行为），麻烦你在真机上确认一下这几点效果。

- 改进：地图上的图钉之前全是同一个紫色圆点，数字代表"第几天"，看着单调，也看不出这几天分别去了哪、怎么走。现在图钉按"第几天"换成不同颜色，同一天的几个点之间连一条线，线上带箭头指向下一站；图钉里的数字改成"当天第几个"（第几天已经用颜色表达了）。
- 新增：地图左上角加了"第1天/第2天/…"的颜色图例，点一下能选中那一天、只看那天的路线，再点一下取消——默认不选中任何一天，不然行程天数一多，所有路线叠在一起会很乱。
- 修复：图钉形状换成真正的水滴图钉（带阴影），不是漂浮的圆点——第一次上线时这部分漏改了，图钉还是旧样式。
- 修复：颜色只有6种，行程超过6天就会撞色，现在准备了30种色相均匀分布的颜色，一趟行程哪怕一个月都不会重复。
- 改进：选中某一天后，地图会自动缩放移动到刚好框住那天的所有点（之前视角一直停在第一天的位置，选的那天如果隔得远——比如跨城市的一日游——线会拉到屏幕外面看不全）；同时其他天的图钉会变浅，更清楚在看哪一天。
- 修复：地图左上角的天数图例挡住了地图自带的放大缩小按钮，现在把放大缩小挪到右下角。
- 修复：图钉编号和连线走向之前没有按实际时间排序，而是按数据库查出来的原始顺序——大多数时候两者刚好一样看不出问题，但顺序一旦不一致（比如中途改过行程项的时间），线就会画歪、图钉编号也对不上实际先后。现在跟时间线视图用的是同一套排序。
- 说明：没有新增任何数据字段，纯粹是地图这一屏怎么画的调整，点开图钉看名字和时间的弹窗跟以前完全一样。

- 新增：行程项（餐厅、周游券、酒店、活动这类需要提前订的）现在可以标一个预约状态——"无需预约"（默认，大多数行程项都是这个，不显示任何东西）、"待预约"、"已预约"。在编辑表单里的"预约状态"选一下就行。
- 新增：标了状态的行程项，卡片上会带一个小圆角徽章（"待预约"琥珀色 / "已预约"绿色），点一下徽章可以直接切换"待预约↔已预约"，不用打开编辑表单——但徽章不会因为点击回到"无需预约"，那个只能进编辑表单里改，算是刻意让"取消预约标记"这个动作更郑重一点。
- 新增：时间线视图切换按钮那一排右边多了个筛选图标，点一下只看"待预约"的行程项，再点一下恢复显示全部。日历视图里也会带同样的徽章，但不加筛选——那个视图本来就是概览用的，管理还是回时间线做。
- 说明：这是"预约待办"功能三阶段路线图里的第一阶段（行程项本身的状态标记）。独立的出行前待办清单、预约临近提醒，都还没做，是各自需要单独讨论确认的后续功能。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0013_itinerary_booking_status.sql`）才能生效。

## 2026-08-18（记一笔改版：全屏页，什么都不用拖）

- 调整：记一笔从底部弹层改成全屏页。之前那个弹层最多只占88%屏幕高度，实测过什么都没填就已经有111px在屏幕外——保存按钮永远要先往下拖一下才碰得到，跨天分摊+外币这类稍微复杂点的账目能拖出接近两屏。
- 新增：阶段、日期、备注、付款人、关联行程这5项常驻可见，不折叠不隐藏；只有"怎么分"和"花在几天"这两项——各自都是"多选+平均/自定义切换+逐项金额+实时校验条"四层堆叠，塞进一屏会把校验提示挤没——改成推进独立子页，校验条固定在子页底部，不会被输入框顶飞。
- 修复：从"记账"列表点进去编辑账目这个入口，之前完全没接安卓返回键（只有右下角"＋"新增那条接了）——弹层时代还有个明显的关闭按钮兜底，这次改成全屏页后一并接上，不然会比之前更不像原生APP。
- 说明：切换"阶段"仍然会清空已选分类（这个行为本来就是防止出行前/途中类目混着选，不是新问题），只是新布局下分类和阶段的位置隔得更远，如果切完阶段没留意，可能会看不出保存按钮为什么点不了；这个和"已记下"确认气泡在全屏页上感觉更突兀这两点，这次先不处理，之后有需要再单独讨论。

## 2026-08-18（修复：「更多」面板里点分享设置/提交反馈/行程动态没反应）

- 修复：从「···」更多面板里点「分享设置」「提交反馈」「行程动态」，面板关掉了但要打开的那个界面根本没出现，看起来就像点了没反应。原因是这几个入口都是"先关掉当前面板、再打开下一个"，而关闭时回收浏览器历史记录的动作是异步的，等它生效时接住的已经是刚打开的那个面板，于是那个面板误以为用户按了返回键、立刻把自己关掉了。现在改成按"有没有面板开着"统一处理，面板之间切换不再触发这个竞态。系统返回键关闭弹层的行为不受影响。

## 2026-08-18（记账新增：住宿/交通这类跨天开销可以摊到每一天）

- 新增：记一笔时可以选"跨多天"，把这笔钱摊到好几天上。以前住宿、周游券这类横跨好几天的开销只能整笔算在某一天头上，导致那天的"当日花费"虚高、其他天虚低，每日数据不反映真实情况。
- 用法：勾选要摊到哪几天（**不要求连续**，比如周游券只在第1天和第4天用就只勾那两天），然后选"平均分到每天"或者"每天自定义"。自定义时跟分摊给家人一样有"刚好分完 ✓ / 还剩X没分完"的实时校验，没分完不让保存。
- 说明：拆分只影响"这笔钱算在哪几天"，不改变总金额，也不改变家人之间怎么分摊——分账结算的数字完全不受影响。老账目一条都不用动，不选"跨多天"的话行为跟以前完全一样。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0011_expense_day_allocation.sql`）才能生效。

## 2026-08-18（一批"让APP更有人味/更像原生APP"的改动）

针对"互动性低、感觉留不住人"这个反馈做的一批改动。核心判断是：这个APP记录侧做得很深，但**回报侧太薄**——用户几乎所有动作都是"我喂它数据"，它几乎不给回什么；而且多人共享数据这件事，在界面上完全没有存在感。

**让"一家人一起用"看得见：**
- 新增：账目行现在会显示"由X记录"（只在记的人和付钱的人不是同一个时才显示，否则纯属噪音）。这个信息一直都在存，只是以前从来没显示过，所以"这笔是家里别人帮我记的"完全看不出来。
- 新增：家里别人记了新账，"记账"tab上会出现一个小红点；进去之后，上次打开之后新增的那几笔会带一圈高亮边，看完自动消失。只标"有新东西"不标具体条数——不想让人觉得"必须逐条处理完"。
- 新增：「更多」面板里加了「行程动态」，按时间倒序列出这趟行程里谁记了账、谁加了行程安排、谁结了账。历史数据没有记录"是谁"的，会显示成"有人"。
- 改进：以前同步是固定30秒一次，静默进行。现在**页面一回到前台就立刻同步一次**（最常见的场景就是"放下手机一阵子再拿起来看家里记了什么"，以前要干等一整个周期）；前台缩到10秒一次，切到后台放宽到60秒省电。真的拉到新数据时，顶部角标会短暂显示"刚更新"。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0012_activity_attribution.sql`）——给行程项和结算记录加上"谁加的"这一列。纯增量、可空、不动现有数据。

**手感更像原生APP：**
- 修复：装到手机主屏幕后，APP四周一直有一圈深色边框、上下各浪费一截屏幕高度——那是当初做设计稿时的"手机样机"外框，一直没针对真机去掉。现在手机上是真正满屏，电脑上仍然保留那个居中的手机框外观（在电脑上它是加分项）。
- 修复：满屏之后顶部内容不会被刘海挡住、底部导航和「＋」按钮不会压到 home indicator 上（正式接入了系统安全区，之前是一个硬编码的近似值）。
- 新增：所有按钮按下去会有一个轻微的回弹反馈。触屏上没有鼠标悬停态，点下去毫无反应会让人怀疑"我到底点到了没有"。
- 新增：记完一笔账会先闪一下"已记下 ✓"再收起面板。以前保存完面板直接消失，动作和结果之间少了一环确认。
- 修复：安卓上装成APP后，弹层开着按系统返回键会直接退出整个APP，现在改成关闭当前弹层。
- 调整：右下角「＋」按钮从文字符号换成了线条图标，和APP其他地方的图标体系统一（文字符号在部分安卓字体下渲染会不一致，是之前那个"弹窗里有奇怪小三角"的同类问题）。
- 所有新增动效都遵循系统的"减少动态效果"设置，开了那个开关就全部退化成瞬间完成。

## 2026-08-16（修复：分享页里行程项时间顺序不对，比如11AM排在9AM前面）

- 修复（真实数据bug）：只读分享页显示的行程项，一直是按"加进去的先后顺序"排的，没有按实际时间排——如果11AM那一项是先加进App的、9AM是后来才加的，分享页就会把11AM排在9AM上面，跟App自己里面看到的顺序不一样。App内部（时间线、日历视图）早就是按时间正确排序的，只有分享页走的是一条单独的数据库查询，这条逻辑一直没同步过去，现在改成一致。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0010_fix_shared_itinerary_order.sql`）才能生效。

## 2026-08-16（修复：新版本发布后，手机上一直卡在旧版本，怎么刷新都没用）

- 修复（重要）：找到了真正的根源——新版本的service worker装完之后，一直卡在"已经装好、等着被叫去接管"的状态，但一直没有代码去叫它接管，导致旧版本会一直服务下去，不管点多少次"检查更新"、刷新多少次都没用，Android和iPhone都受影响。现在改成新版本装完就立刻自动接管所有已经打开的页面，配合已有的自动刷新逻辑，之后应该不需要再手动干预。
- 一次性说明：这个bug是"卡住"之后自己解不开的死结——手机上现在跑着的还是有bug的旧版本代码，它不知道怎么正确接过新版本，需要手动清一次浏览器里这个网站的数据（会重新登录一次，行程数据在云端是安全的，重新打开会自动同步回来）才能彻底摆脱。**只需要做这一次**，之后的更新应该会自动生效。

## 2026-08-16（修复："检查更新"点了经常感觉没生效）

- 修复：点"更多"面板里的"检查更新"，之前会在新版本真正装完接管页面之前就抢先刷新，刷出来的大概率还是旧版本的缓存页面，看起来像点了没反应。现在会等新版本真正接管之后才刷新（最多等4秒，没有新版本时行为不变）。

## 2026-08-16（行程项新增"备注"）

- 新增：行程项编辑时可以填"备注"了（数据库字段本来就有，这次补上界面）。用一个可以手动拖高的多行文本框，多个提醒事项自己换行或者加"1. 2. 3."分点就行，不用额外的加点/删点操作。
- 界面细节：行程列表里备注默认完整显示全部内容（一开始做过"最多2行+省略号截断"，用户反馈想直接看到全部，已经改掉）。没填备注的行程项不会多出空白区域。

## 2026-08-16（登录方式改成邮箱验证码，不再是登录链接）

- 修复：原来的邮箱登录链接，如果在跟发起登录不同的浏览器/App里打开（比如手机邮箱App默认用系统浏览器打开链接，或者在微信/小红书这类App内置浏览器里点开网站），会必定登录失败——这是链接登录机制本身的限制，不同浏览器/App之间无法共享登录发起时的本地状态。现在改成邮箱收到一个6位数字验证码，回到原来那个页面手填进去就能登录，不再依赖"链接在哪打开"，从根上避开这个问题。
- 体验细节：验证码填满第6位自动验证，不用再点确认；支持整段粘贴自动分配到6个格子；验证码错了原地提示，不用退回重填邮箱；30秒后才能重新发送，防止连续点击刷邮箱。
- 部署要求：已在 Supabase 后台把邮件模板从登录链接改成验证码格式（Authentication → Emails → Magic link or OTP），验证码5分钟内有效。

## 2026-08-16（安装提示：识别iOS上的Chrome等第三方浏览器，给出正确指引）

- 修复：iOS上用Chrome/Firefox/Edge等浏览器打开时，之前的安装提示一律显示"在Safari点分享→添加到主屏幕"——但iOS只有Safari本身能把网站真正装成独立APP，其他浏览器就算有类似菜单项，点了也只是加个普通网页书签，不会有真正的APP体验，这是苹果的平台限制。现在会识别出是iOS上的第三方浏览器，改为提示"请用Safari重新打开这个网址"，不再给出对这些浏览器无效的操作步骤。

## 2026-08-16（修复：刚开启分享/切换模板后，预览可能显示"链接打不开"或旧模板）

- 修复（真实数据bug）：刚开启分享链接、或者刚切换了分享模板，马上点"预览"，有几率显示"这个链接打不开"，或者预览出来的还是切换之前的旧模板——原因是分享设置的改动先存在手机本地，要等一小段时间才会真正同步到服务器，而"预览"看到的内容来自服务器，中间这个空档期就会看到过期/缺失的数据。现在改成范围/模板一改完就立刻尝试同步，同步进行中"复制链接/预览"那块会显示"正在同步到服务器…"，等真正同步完成才能点，不会再看到过期内容。

## 2026-08-16（修复：部分输入框的提示文字在手机上会被截断）

- 修复：地点、行程名称、目的地国家这几个输入框，提示文字比较长，手机窄屏幕下只能看到前半段（输入框的提示文字本身不会换行，这是限制）。现在提示只保留最短的"这里要填什么"，举例/补充说明挪到输入框下面单独一行，不会再被截断。

## 2026-08-16（地点搜索新增：贴Google Maps链接兜底）

- 新增：记行程时的地点搜索框，如果搜不到（或者搜出来文不对题——免费地图数据源对小型酒店/民宿/餐厅这类商家收录不全），现在可以直接把Google Maps的分享链接贴进去，APP会解析出链接里的精确坐标直接定位，不用再靠搜索。
- 说明：这个不需要申请任何第三方付费API，纯粹是解析一个链接，完全免费。

## 2026-08-16（修复：多人分摊账目的分摊明细可能永远同步不上去，一直卡"待同步"）

- 修复（重要，真实数据bug）：记多人分摊的账目时，分摊明细以前是一行一行单独推送到云端的，而数据库有一道"分摊总额必须等于账目总额"的保护检查——单独推一行时，总额还没凑齐就先被这道检查拦下来，这一行就永远同步不上去，"待同步"计数因此长期卡住不清零，其他设备也永远收不到这几行完整的分摊数据（虽然自己手机上因为是本地数据，看起来账算得对，但换手机或团队里其他人的手机看到的分摊会不完整）。现在改成把同一笔账的所有分摊明细打包一起、原子性地推送，不会再被这道检查提前拦下来；之前已经卡住的记录也会在这次更新后自动补推成功，不需要手动处理。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0009_atomic_expense_split_push.sql`）才能生效。

## 2026-08-16（修复：记账"只勾1个非付款人"分摊算错 + iOS安装提示可能被盖住）

- 修复（重要，真实资金bug）：记一笔时选了付款人（比如"BX垫付"）、"大家分摊"模式下却只勾了另外1个人（比如只勾"KN"），这笔钱之前会被错误地整笔记回付款人自己名下，"分账"结算里完全看不出这笔钱其实是该由被勾的那个人承担——这笔账看起来凭空"没有人欠钱"。现在会正确记成被勾选的那个人欠付款人这笔钱。同时修复了这类记录编辑时会被误判成"个人开销"页签、看不到原本勾选的人的问题，记账列表这类记录也不会再被标成容易误解的"个人开销"。
- 修复：iOS（Safari）安装到主屏幕的提示条，在部分较新机型上会被系统底部悬浮工具栏/主屏幕指示条盖住，看起来像完全没有弹出提示——现在提示条会避开这块区域。iOS本身不支持像安卓那样系统自动询问"是否安装"，仍然需要手动点分享→添加到主屏幕，这条提示只是引导这一步。

## 2026-08-16（版本号显示 + 手动检查更新）

- 新增："更多"面板里加了"检查更新"，显示当前版本（git短SHA + 构建时间），反馈问题时可以直接说清楚是哪个版本。
- 新增：点"检查更新"会先问一下有没有新版本，然后无论有没有都强制刷新一次页面——手机装成APP之后没有浏览器的刷新按钮，下拉刷新手势也关掉了，这个补上"我想手动刷新一下"这个能力，刷新后版本号有没有变就是"刚才有没有生效"的证据。
- 新增：提交反馈时自动带上当前版本号，不用自己说清楚，反馈记录里直接看得到。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0008_feedback_app_version.sql`）才能生效。

## 2026-08-16（分享模板选择改版：缩略图网格 + 预览按钮）

- 调整：分享设置里选模板，从10行纯文字列表改成2列缩略图网格，每套模板配一个还原真实配色/版式的小图，不用再靠名字和一行描述"盲猜"效果。
- 新增：选好模板、生成链接之后，"复制链接"旁边多了一个"预览"按钮，点了直接在新标签页打开这个真实分享链接——发给朋友之前，自己先看一眼真实效果。

## 2026-08-16（顶部常驻分享状态徽章）

- 新增：顶部（跟"已同步"同一排）现在常驻一个分享状态入口，不用再钻进"···"更多菜单才能找到"分享设置"。没开启分享时是一个不起眼的灰色链接图标；一旦开启，会变成醒目的"分享中"标签，只要还开着就会一直挂在那里提醒你，点一下都能直接打开分享设置管理。
- 说明："···"菜单里原来的"分享设置"入口保留，两条路都能到同一个地方。

## 2026-08-16（记账页新增"我的花费"视角）

- 新增：记账页顶部可以切换"团队视角/我的花费"。"我的花费"显示你这趟行程实际要承担的总额（不管是自己付的、还是分摊别人垫付的都算进去），下面的账目列表也会跟着只显示跟你有关的账目，每一笔还会标出"你的份额"是多少。
- 说明：预算页暂时还是团队视角，没有加个人预算这个维度——这次先解决"我到底花了多少钱"这个问题。

## 2026-08-16（记账分摊新增"自定义金额"，不再强制平分）

- 新增：记一笔时，"这笔怎么算"选"大家分摊"之后，多了"平均分摊/自定义金额"两个选项。选"自定义金额"，每个人旁边可以自己填具体分摊多少钱（默认先按平分帮你填好，方便直接改），没填够/超出总金额时会提示、也没法保存，避免手滑分错钱。
- 说明：这次不涉及数据库改动——数据库从最初设计时就预留了这个分摊方式，只是之前一直没有对应的界面。

## 2026-08-16（团队邀请码：邀请家人朋友加入不用再手动跑SQL）

- 新增：点顶部身份切换 → "邀请新成员加入团队"，可以看到/复制自己团队的邀请码，发给想邀请的家人朋友即可。邀请码泄露了可以随时"重新生成"作废重来。
- 新增：登录页如果邮箱提示"还没被邀请"，下面多了"有邀请码？点这里输入"——对方直接在这里输入邮箱+邀请码就能自己加入团队，不用再联系我们手动开通。
- 说明："陌生人自己创建一个全新团队"（不是加入你已有的团队）这个功能暂时不做，还是维持现在的人工流程；这次只解决"邀请人加入我已有的团队"这一半。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0007_household_invite_code.sql`）才能生效。

## 2026-08-16（修复："进行中"状态不会自动更新）

- 修复：行程列表里"规划中/进行中/已结束"这个标签，以前是新建行程那一刻算好就写死的，之后不管日期怎么过去、行程早就结束了几个月，都会一直显示"进行中"。现在改成每次打开都用当天日期实时判断：没到开始日期→规划中，在开始~结束日期之间→进行中，过了结束日期→已结束。
- 说明："已归档"这个状态目前还没有对应的功能（没有任何地方能把行程手动设成这个状态），这次不处理，之后如果要做"手动收起旧行程"再另外讨论。

## 2026-08-16（取消整站密码墙）

- 调整：打开网址不再需要先输入一道共用密码——现在直接看到邮箱登录界面。真正的数据边界一直都是"邮箱必须先被邀请 + 每个团队只能看到自己的数据"（这两条没有变化），密码墙其实是邮箱邀请机制上线之前的临时措施，现在正式拿掉，方便招募新的测试用户时少一道门槛。
- 说明：没被邀请的邮箱依然进不去任何数据，只是能看到登录界面本身。

## 2026-08-16（修复：本地行程数据对不上时永久白屏）

- 修复：如果手机上记的"当前行程"在数据库里已经找不到了（比如换设备、行程被删、或者任何原因数据没对上），APP以前会卡在一片空白，没有任何提示，也不会自动回到"选行程"的界面。现在会自动识别这种情况，清掉记错的行程、跳回"我的行程"列表。
- 整理：新增一份可复用的本地测试数据（多个行程、多个成员、含停用成员、含刻意模拟的老数据），只在本地开发环境生效，不影响真机/线上，方便以后每次改动先用真实感数据验证一遍，而不是每次现场造一个干净的新行程。

## 2026-08-13（只读分享链接，第二批：补齐全部10套模板）

- 新增：分享页模板从3套补齐到10套——新增旅记手账风、路线地图感、旅程结算单、出发信息板、旅行纪事报、亲子色块、夜航玻璃共7套，风格互不重复（复古票根、报纸排版、机场信息板、亲子色块卡片、深色玻璃质感等），"分享设置"里现在10套都能直接选，不再有"即将推出"的占位项。

## 2026-08-13（修复：部分手机上看不到"分享设置"）

- 修复：行程"更多"面板（导出与分享）以前没有滚动限制，内容一多就会超出屏幕高度，超出的部分既看不见也没法滚动上去——真机测试发现"分享设置"和"提交反馈"这两行在部分手机屏幕上因此完全看不到。现在这个面板跟其他面板一样，超出屏幕高度时可以正常上下滑动查看。

## 2026-08-13（只读分享链接，第一批：基础设施 + 3套模板）

- 新增：行程"更多"面板里新增"分享设置"，可以生成一个只读分享链接——拿到链接的人不用登录就能看行程和/或花费汇总（自己选范围：仅行程/仅花费/两者都有），但不能编辑。花费只给汇总数字，不会暴露每一笔明细。
- 新增：分享页有3套视觉模板可选（车票凭证感、时刻表编辑感、旅途拼贴），之后还会陆续再加7套。
- 说明：第一次把分享范围从"关闭"改成其他任何值时，会有一个确认弹窗明确说清楚"任何拿到链接的人都能看到什么"，不会因为打开设置面板就默默生效。
- 说明：可以随时"重新生成链接"作废旧链接（比如担心分享错人了）。
- 部署要求：需要先在 Supabase 后台跑一次新的数据库迁移（`0006_public_share.sql`）才能生效，这次开发只做完了基础链路和3套模板，其余7套模板陆续再补。

## 2026-08-13（第三批：弹层/下拉健壮性加固）

- 修复：地点搜索的下拉建议框，如果输入后很快点别处关闭，几百毫秒后姗姗来迟的搜索结果会把已经关掉的下拉又自动弹出来——现在关闭之后的搜索结果会被正确丢弃。
- 修复：顶部身份切换下拉里，如果点了"编辑"某个成员、还没保存或取消就直接点切换成另一个人，下次重新打开下拉，刚才编辑到一半的那一行会莫名其妙又变回编辑状态——现在这两条关闭路径统一处理，不会再留下这种"半吊子"状态。
- 加固：所有弹层/下拉（确认框、汇率簿的"另存为新标签"、导出/反馈/记账几个底部面板、身份切换、日期/时间/地点/国家选择器）现在都支持按 Escape 键关闭。
- 加固：记一笔的保存/删除按钮加了防抖，快速连续点两下不会再触发两次并发的保存/删除请求。
- 整理：汇率簿里"另存为新标签"的弹层原来是复制粘贴确认框的代码，现在跟确认框共用同一个组件，以后两边不会再走着走着变得不一致。

## 2026-08-13（第二批：更新检测 + 登录状态修复）

- 修复（重要）：真机测试发现，把APP完全关闭再重新打开，有几率被要求重新输入邮箱登录，即使几分钟前还在正常使用。原因是APP启动时同时用了两种方式判断"有没有登录"，在真正冷启动的瞬间可能抢跑，把"客户端还没读完本地登录信息"误判成"没有登录"。现在只依赖 Supabase 官方提供的、保证不会抢跑的判断方式。
- 修复：以前APP只有在完全关闭重新打开时才会检查有没有新版本，切到后台再切回来是不会检查的，导致每次更新后必须完全关闭重开才能用上新版本。现在APP会每隔一段时间自动检查一次，而且一切回到前台就立刻检查，测试用户正常切换APP就能自动用上最新版本，不需要专门关闭重开。

## 2026-08-13（相邻地点通勤提示）

- 新增：行程"时间线"和"日历"视图里，相邻两个行程项之间会显示一行真实步行路线的距离和时长（如"步行12分钟 · 900米"），数据来自 OpenRouteService 的真实道路路线，不是直线距离估算。这一行可以直接点击，跳转到 Google Maps 显示这两个地点之间的路线（不锁定步行/开车/公交，自己在地图里选）——跳转动作用图标而不是文字，跟全APP一贯的风格保持一致。
- 说明：如果相邻地点里有一个还没设置具体坐标（只手打了地址文字，没从下拉建议里选），会显示"位置未知"，没有链接；如果距离太远（超过3公里，比如跨城市），不会再显示不切实际的步行时长，只显示"相距X公里"；如果路线服务暂时调用失败，会降级显示一个"在地图中查看路线"的纯跳转链接，而不是完全不显示。
- 部署要求：需要在 Vercel 后台配置 `ORS_API_KEY` 环境变量才会显示真实步行时长（没配置的话，能跳转地图的链接仍然可用），见 `docs/deployment.md`。
- 修复：日历视图里同一天的行程项以前没有按时间顺序排列（用的是数据库查询的原始顺序），现在跟时间线视图统一用同一套排序逻辑。
- 修复：本地开发时如果没配置 Supabase（比如临时测试环境），新建家庭成员/行程等操作会静默失败没有任何提示——现在这种情况下会用一个固定的本地测试团队ID，让纯本地模式真正可用。

## 2026-08-11（第四批：家庭成员支持编辑/停用）

- 新增：点顶部身份切换器，每个家庭成员名字旁边可以编辑名字；删除的时候，如果这个人从来没记过账，直接删掉；如果已经有记账/分摊/结算记录，会改成"停用"（不删数据，只是不再出现在选身份/记账的名单里，历史记录不受影响），并且可以在"已停用的成员"里随时恢复。
- 修复：被停用的成员不会再出现在新记一笔账的"付款人/分摊给"名单里，但编辑他之前记过的旧账目时，还是能看到他。

## 2026-08-11（第三批：按钮文字换图标）

- 界面调整：全APP范围内，"保存/取消/删除/编辑/关闭/添加/归档/恢复"等重复出现的动作按钮，统一换成线条图标（跟日期/时间选择器已经在用的风格一致），减少文字堆叠、看起来更清爽。像"分享 ›""去反馈 ›""大家分摊/个人开销"这类不容易用单个图标说清楚的按钮，保留了文字。

## 2026-08-11（第二批：真机试用反馈修复）

- 新增：顶部标题栏可以直接看到自己当前的身份、点一下弹出成员列表切换身份或添加新家庭成员（不用再钻进"⋯"更多菜单，也不用回到最开始那一屏）。
- 修复：顶部一直显示"9:41"的假状态栏文字，已删除（真手机本身就有自己的状态栏）。
- 修复：行程时间显示到秒（如"22:15:00"），现在统一显示"22:15"。
- 调整：Android 手机点"分享"文件——原来经常直接变成下载、不弹出系统分享面板，现在改成"先生成文件、再点一次才真正分享"的两步操作，并且如果还是没能分享成功，会直接显示具体失败原因，方便继续排查（个别手机的浏览器版本可能确实不支持分享文件，这种情况下"下载"是目前能做到的最好效果）。
- 修复：记账弹窗里的日期/时间选择器，把可能在部分安卓手机上显示成奇怪小三角的表情符号图标，换成了正常的线条图标。
- 修复：手机上下拉刷新会导致页面跳回"行程"页，已关闭这个下拉刷新手势（APP本身会自动同步，不需要靠下拉刷新）。

## 2026-08-11

- 新增：云端同步正式上线并修复——多台设备之间的行程、账目数据现在会自动同步到 Supabase，此前手机上"一直显示待同步"的问题已修复（原因是 Vercel 后台配置的 Supabase 密钥中混入了看不见的错误字符）。
- 新增：整站密码墙——部署到 Vercel 后，任何人打开网址前必须先输入账号密码，防止陌生人直接读写数据。
- 部署：APP 正式上线，网址 https://travelapp-kappa-wheat.vercel.app 。
