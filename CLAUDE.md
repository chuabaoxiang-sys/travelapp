# 旅记 / TripJournal — 协作规矩

这个文件是给 Claude 看的硬规则，不是文档。每次会话都会无条件加载，优先于默认行为。

---

## 最重要的一条：动手前必须先问

**每一项改动开工之前，单独跟用户讲清楚方案、拿到明确的"可以"，才能开始写代码。**

用户 2026-08-13 的原话：

> 不要同时开发3项功能，一个一个来，每个功能开发前，都需要和我进行讨论，让我确认方案，然后才可以实行开发，我不要你直接就开发！

这条适用于**所有**改动，不只是界面——后端逻辑、数据库、同步机制，一样要先问。理由是用户明确说过的：「我不想浪费token来回开发」，所以关卡要设在写代码**之前**，不是写完再改。

### 三个已经踩过的坑（都是真实发生过的失败，不是假设）

**1. 计划批准 ≠ 逐项批准。**
用户批准一份包含多项的计划（ExitPlanMode），**只代表方向对**，不代表可以一口气把里面每一项都做完。
2026-08-18 就是这么翻车的：一份计划里 8 项（B1–B5、A1–A4），批准之后全部批量做完，中间一次都没停。
正确做法是：计划批准 → 讲第1项方案 → 等确认 → 做第1项 → 讲第2项 → …… 每一项都要单独过。

**2. 涉及外观的改动，要先出对比图，不是写完再补。**
先做一份能看的东西（HTML mockup 或前后对比，发布成 Artifact），拿到"可以"再写实现代码。
出对比图时要**用项目真实的设计 token 渲染**（`src/index.css` 里的 `@theme` 那一段），不要用示意色块——用户要看的是真实效果。

**3. "这不算设计改动吧" —— 这个念头本身就是信号。**
当你心里在想"我只是清理一个遗留物 / 只是用现成组件拼一下 / 只是改个图标"，说明你正在做的**就是**需要先问的那类改动。
真实案例：去掉手机样机外框被我归类成"清理遗留物"、tab 红点被归类成"用现成组件拼装"——两个都改变了用户每天看到的东西，两个都应该先问。
判断标准很简单：**用户打开APP会不会注意到不一样了？** 会，就先问。

---

## 设计语言是定稿的，不要自己发挥

配色 token、字体、圆角体系、分类色、底部四个 tab 的结构，全部来自用户在 claude.ai/design 上定稿的参考项目。
- 精确数值看 `src/index.css` 的 `@theme` 块和计划文件里的「视觉设计语言」章节，**不要凭记忆重新推导调色板**。
- 要加新的视觉元素，优先用现有 token 拼装；确实需要引入新颜色/新圆角时，这本身就是要先问的改动。
- 按钮优先用 lucide 图标，不要用纯文字标签，也不要用 emoji/全角符号当图标（安卓字体渲染不一致，踩过坑）。

---

## 其他固定动作

- **每次用户能感知的改动，都要记进 `CHANGELOG.md`**，用大白话写"改了什么、为什么"，不写内部代码整理。
- **每个 CRUD 功能必须一次性做齐增/改/删**，不要分批上（用户明确要求过）。删除要先判断有没有关联数据，有就改成"停用"而不是硬删。
- **排查问题时主动要截图和控制台输出**，不要靠猜。
- **数据库迁移**走 `supabase-migration-safety` skill；**邀请新团队**走 `invite-team` skill；**部署后**走 `deploy-verification-checklist` skill。

---

## 环境事实（省得每次重新查）

- 生产 Supabase：`pivhpufmgmcazztlshfw`；测试 Supabase：`jwngufyqkdgwawsirdsl`。**两个是完全独立的项目，任何后台设置都不会互相同步。**
- 线上地址：`https://travelapp-kappa-wheat.vercel.app`，推 `main` 自动部署。
- 迁移 SQL 一律由用户自己贴进 Supabase SQL Editor 执行，`service_role` 密钥**绝不出现在任何运行中的代码里**。
- 安全区（刘海/home 条）只能真机验证——浏览器里 `env(safe-area-inset-*)` 永远是 0。
