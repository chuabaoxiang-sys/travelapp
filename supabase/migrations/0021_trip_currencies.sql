-- =====================================================================
-- 0021_trip_currencies.sql
-- 行程新增"会用到的货币"（本位币之外，预计还会用到的外币），记一笔时
-- 金额旁边的币种chip从这个列表+本位币生成，不用每次手动打字选。
--
-- 纯新增、可空，不影响任何已有数据：老行程这一列是 null，前端按空数组
-- 处理，记一笔时退回成"只有本位币一个chip"——不会比现在更差。
-- 跟 0003_sync_fixes.sql 里 destination_countries 是同一个套路（同一张
-- 表新增一个可选的 text[] 字段），沿用同样的写法。
-- =====================================================================

alter table trip add column if not exists currencies text[];

comment on column trip.currencies is
  '本位币之外，这趟行程预计会用到的外币（ISO 4217 大写代码数组）。记一笔时用来生成'
  '快捷选择的币种chip，不用每次手动输入。';
